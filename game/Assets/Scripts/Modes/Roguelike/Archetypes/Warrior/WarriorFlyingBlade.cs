using UnityEngine;

namespace Game.Modes.Roguelike.Archetypes.Warrior
{
  /// <summary>
  /// Legacy stub — replaced by OrbitWeaponProjectile in Phase 1 rework.
  /// Retained for backward compatibility only. No behavior remains.
  /// </summary>
  public sealed class WarriorFlyingBlade : MonoBehaviour
  {
    public static void Spawn(Vector3 position, Vector3 direction, GameObject owner, object unused) { }
  }
}
